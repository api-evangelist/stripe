naftiko: 1.0.0-alpha2
info:
  label: Stripe Checkout API — Items
  description: 'Stripe Checkout API — Items. 1 operations. Lead operation: Stripe Get   Checkout Sessions Session Line Items.
    Self-contained Naftiko capability covering one Stripe business surface.'
  tags:
  - Stripe
  - Items
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    STRIPE_API_KEY: STRIPE_API_KEY
capability:
  consumes:
  - type: http
    namespace: checkout-items
    baseUri: https://api.stripe.com
    description: Stripe Checkout API — Items business capability. Self-contained, no shared references.
    resources:
    - name: v1-checkout-sessions-session-line_items
      path: /v1/checkout/sessions/{session}/line_items
      operations:
      - name: getcheckoutsessionssessionlineitems
        method: GET
        description: Stripe Get   Checkout Sessions Session Line Items
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: ending_before
          in: query
          type: string
          description: A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list.
            For instance, if you make a list request and receive 100 ob
        - name: expand
          in: query
          type: array
          description: Specifies which fields in the response should be expanded.
        - name: limit
          in: query
          type: integer
          description: A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default
            is 10.
        - name: session
          in: path
          type: string
          required: true
        - name: starting_after
          in: query
          type: string
          description: A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list.
            For instance, if you make a list request and receive 100 o
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
    authentication:
      type: bearer
      token: '{{env.STRIPE_API_KEY}}'
  exposes:
  - type: rest
    namespace: checkout-items-rest
    port: 8080
    description: REST adapter for Stripe Checkout API — Items. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/v1/checkout/sessions/{session}/line-items
      name: v1-checkout-sessions-session-line-items
      description: REST surface for v1-checkout-sessions-session-line_items.
      operations:
      - method: GET
        name: getcheckoutsessionssessionlineitems
        description: Stripe Get   Checkout Sessions Session Line Items
        call: checkout-items.getcheckoutsessionssessionlineitems
        with:
          ending_before: rest.ending_before
          expand: rest.expand
          limit: rest.limit
          session: rest.session
          starting_after: rest.starting_after
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: checkout-items-mcp
    port: 9090
    transport: http
    description: MCP adapter for Stripe Checkout API — Items. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: stripe-get-checkout-sessions-session
      description: Stripe Get   Checkout Sessions Session Line Items
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: checkout-items.getcheckoutsessionssessionlineitems
      with:
        ending_before: tools.ending_before
        expand: tools.expand
        limit: tools.limit
        session: tools.session
        starting_after: tools.starting_after
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
