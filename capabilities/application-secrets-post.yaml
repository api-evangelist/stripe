naftiko: 1.0.0-alpha2
info:
  label: Stripe Application Secrets API — Post
  description: 'Stripe Application Secrets API — Post. 2 operations. Lead operation: Stripe Post Apps Secrets. Self-contained
    Naftiko capability covering one Stripe business surface.'
  tags:
  - Stripe
  - Post
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    STRIPE_API_KEY: STRIPE_API_KEY
capability:
  consumes:
  - type: http
    namespace: application-secrets-post
    baseUri: https://api.stripe.com
    description: Stripe Application Secrets API — Post business capability. Self-contained, no shared references.
    resources:
    - name: v1-apps-secrets
      path: /v1/apps/secrets
      operations:
      - name: postappssecrets
        method: POST
        description: Stripe Post Apps Secrets
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    - name: v1-apps-secrets-delete
      path: /v1/apps/secrets/delete
      operations:
      - name: postappssecretsdelete
        method: POST
        description: Stripe Post Apps Secrets Delete
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.STRIPE_API_KEY}}'
  exposes:
  - type: rest
    namespace: application-secrets-post-rest
    port: 8080
    description: REST adapter for Stripe Application Secrets API — Post. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/v1/apps/secrets
      name: v1-apps-secrets
      description: REST surface for v1-apps-secrets.
      operations:
      - method: POST
        name: postappssecrets
        description: Stripe Post Apps Secrets
        call: application-secrets-post.postappssecrets
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/v1/apps/secrets/delete
      name: v1-apps-secrets-delete
      description: REST surface for v1-apps-secrets-delete.
      operations:
      - method: POST
        name: postappssecretsdelete
        description: Stripe Post Apps Secrets Delete
        call: application-secrets-post.postappssecretsdelete
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: application-secrets-post-mcp
    port: 9090
    transport: http
    description: MCP adapter for Stripe Application Secrets API — Post. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: stripe-post-apps-secrets
      description: Stripe Post Apps Secrets
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: application-secrets-post.postappssecrets
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: stripe-post-apps-secrets-delete
      description: Stripe Post Apps Secrets Delete
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: application-secrets-post.postappssecretsdelete
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
