naftiko: 1.0.0-alpha2
info:
  label: Stripe Identity API — Cancel
  description: 'Stripe Identity API — Cancel. 1 operations. Lead operation: Stripe Post   Identity Verification Sessions Session
    Cancel. Self-contained Naftiko capability covering one Stripe business surface.'
  tags:
  - Stripe
  - Cancel
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    STRIPE_API_KEY: STRIPE_API_KEY
capability:
  consumes:
  - type: http
    namespace: identity-cancel
    baseUri: https://api.stripe.com
    description: Stripe Identity API — Cancel business capability. Self-contained, no shared references.
    resources:
    - name: v1-identity-verification_sessions-session-cancel
      path: /v1/identity/verification_sessions/{session}/cancel
      operations:
      - name: postidentityverificationsessionssessioncancel
        method: POST
        description: Stripe Post   Identity Verification Sessions Session Cancel
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: session
          in: path
          type: string
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
    authentication:
      type: bearer
      token: '{{env.STRIPE_API_KEY}}'
  exposes:
  - type: rest
    namespace: identity-cancel-rest
    port: 8080
    description: REST adapter for Stripe Identity API — Cancel. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/v1/identity/verification-sessions/{session}/cancel
      name: v1-identity-verification-sessions-session-cancel
      description: REST surface for v1-identity-verification_sessions-session-cancel.
      operations:
      - method: POST
        name: postidentityverificationsessionssessioncancel
        description: Stripe Post   Identity Verification Sessions Session Cancel
        call: identity-cancel.postidentityverificationsessionssessioncancel
        with:
          session: rest.session
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: identity-cancel-mcp
    port: 9090
    transport: http
    description: MCP adapter for Stripe Identity API — Cancel. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: stripe-post-identity-verification-sessions
      description: Stripe Post   Identity Verification Sessions Session Cancel
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: identity-cancel.postidentityverificationsessionssessioncancel
      with:
        session: tools.session
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
