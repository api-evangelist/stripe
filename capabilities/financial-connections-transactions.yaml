naftiko: 1.0.0-alpha2
info:
  label: Stripe Financial Connections API — Transactions
  description: 'Stripe Financial Connections API — Transactions. 2 operations. Lead operation: Stripe Get   Financial Connections
    Transactions. Self-contained Naftiko capability covering one Stripe business surface.'
  tags:
  - Stripe
  - Transactions
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    STRIPE_API_KEY: STRIPE_API_KEY
capability:
  consumes:
  - type: http
    namespace: financial-connections-transactions
    baseUri: https://api.stripe.com
    description: Stripe Financial Connections API — Transactions business capability. Self-contained, no shared references.
    resources:
    - name: v1-financial_connections-transactions
      path: /v1/financial_connections/transactions
      operations:
      - name: getfinancialconnectionstransactions
        method: GET
        description: Stripe Get   Financial Connections Transactions
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: account
          in: query
          type: string
          description: The ID of the Stripe account whose transactions will be retrieved.
          required: true
        - name: ending_before
          in: query
          type: string
          description: A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list.
            For instance, if you make a list request and receive 100 ob
        - name: expand
          in: query
          type: array
          description: Specifies which fields in the response should be expanded.
        - name: limit
          in: query
          type: integer
          description: A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default
            is 10.
        - name: starting_after
          in: query
          type: string
          description: A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list.
            For instance, if you make a list request and receive 100 o
        - name: transacted_at
          in: query
          type: string
          description: A filter on the list based on the object `transacted_at` field. The value can be a string with an integer
            Unix timestamp, or it can be a dictionary with the fol
        - name: transaction_refresh
          in: query
          type: object
          description: 'A filter on the list based on the object `transaction_refresh` field. The value can be a dictionary
            with the following options:'
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
    - name: v1-financial_connections-transactions-transaction
      path: /v1/financial_connections/transactions/{transaction}
      operations:
      - name: getfinancialconnectionstransactionstransaction
        method: GET
        description: Stripe Get   Financial Connections Transactions Transaction
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: expand
          in: query
          type: array
          description: Specifies which fields in the response should be expanded.
        - name: transaction
          in: path
          type: string
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
    authentication:
      type: bearer
      token: '{{env.STRIPE_API_KEY}}'
  exposes:
  - type: rest
    namespace: financial-connections-transactions-rest
    port: 8080
    description: REST adapter for Stripe Financial Connections API — Transactions. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/v1/financial-connections/transactions
      name: v1-financial-connections-transactions
      description: REST surface for v1-financial_connections-transactions.
      operations:
      - method: GET
        name: getfinancialconnectionstransactions
        description: Stripe Get   Financial Connections Transactions
        call: financial-connections-transactions.getfinancialconnectionstransactions
        with:
          account: rest.account
          ending_before: rest.ending_before
          expand: rest.expand
          limit: rest.limit
          starting_after: rest.starting_after
          transacted_at: rest.transacted_at
          transaction_refresh: rest.transaction_refresh
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/v1/financial-connections/transactions/{transaction}
      name: v1-financial-connections-transactions-transaction
      description: REST surface for v1-financial_connections-transactions-transaction.
      operations:
      - method: GET
        name: getfinancialconnectionstransactionstransaction
        description: Stripe Get   Financial Connections Transactions Transaction
        call: financial-connections-transactions.getfinancialconnectionstransactionstransaction
        with:
          expand: rest.expand
          transaction: rest.transaction
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: financial-connections-transactions-mcp
    port: 9090
    transport: http
    description: MCP adapter for Stripe Financial Connections API — Transactions. One tool per consumed operation, routed
      inline through this capability's consumes block.
    tools:
    - name: stripe-get-financial-connections-transactions
      description: Stripe Get   Financial Connections Transactions
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: financial-connections-transactions.getfinancialconnectionstransactions
      with:
        account: tools.account
        ending_before: tools.ending_before
        expand: tools.expand
        limit: tools.limit
        starting_after: tools.starting_after
        transacted_at: tools.transacted_at
        transaction_refresh: tools.transaction_refresh
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: stripe-get-financial-connections-transactions-2
      description: Stripe Get   Financial Connections Transactions Transaction
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: financial-connections-transactions.getfinancialconnectionstransactionstransaction
      with:
        expand: tools.expand
        transaction: tools.transaction
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
