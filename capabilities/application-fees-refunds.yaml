naftiko: 1.0.0-alpha2
info:
  label: Stripe Application Fees API — Refunds
  description: 'Stripe Application Fees API — Refunds. 1 operations. Lead operation: Stripe List Application Fee Refunds.
    Self-contained Naftiko capability covering one Stripe business surface.'
  tags:
  - Stripe
  - Refunds
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    STRIPE_API_KEY: STRIPE_API_KEY
capability:
  consumes:
  - type: http
    namespace: application-fees-refunds
    baseUri: https://api.stripe.com
    description: Stripe Application Fees API — Refunds business capability. Self-contained, no shared references.
    resources:
    - name: v1-application_fees-id-refunds
      path: /v1/application_fees/{id}/refunds
      operations:
      - name: getapplicationfeesidrefunds
        method: GET
        description: Stripe List Application Fee Refunds
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: ending_before
          in: query
          type: string
          description: A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list.
            For instance, if you make a list request and receive 100 ob
        - name: expand
          in: query
          type: array
          description: Specifies which fields in the response should be expanded.
        - name: id
          in: path
          type: string
          required: true
        - name: limit
          in: query
          type: integer
          description: A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default
            is 10.
        - name: starting_after
          in: query
          type: string
          description: A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list.
            For instance, if you make a list request and receive 100 o
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
    authentication:
      type: bearer
      token: '{{env.STRIPE_API_KEY}}'
  exposes:
  - type: rest
    namespace: application-fees-refunds-rest
    port: 8080
    description: REST adapter for Stripe Application Fees API — Refunds. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/v1/application-fees/{id}/refunds
      name: v1-application-fees-id-refunds
      description: REST surface for v1-application_fees-id-refunds.
      operations:
      - method: GET
        name: getapplicationfeesidrefunds
        description: Stripe List Application Fee Refunds
        call: application-fees-refunds.getapplicationfeesidrefunds
        with:
          ending_before: rest.ending_before
          expand: rest.expand
          id: rest.id
          limit: rest.limit
          starting_after: rest.starting_after
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: application-fees-refunds-mcp
    port: 9090
    transport: http
    description: MCP adapter for Stripe Application Fees API — Refunds. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: stripe-list-application-fee-refunds
      description: Stripe List Application Fee Refunds
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: application-fees-refunds.getapplicationfeesidrefunds
      with:
        ending_before: tools.ending_before
        expand: tools.expand
        id: tools.id
        limit: tools.limit
        starting_after: tools.starting_after
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
