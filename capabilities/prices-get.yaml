naftiko: 1.0.0-alpha2
info:
  label: Stripe Prices API — Get
  description: 'Stripe Prices API — Get. 3 operations. Lead operation: Stripe Get   Prices. Self-contained Naftiko capability
    covering one Stripe business surface.'
  tags:
  - Stripe
  - Get
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    STRIPE_API_KEY: STRIPE_API_KEY
capability:
  consumes:
  - type: http
    namespace: prices-get
    baseUri: https://api.stripe.com
    description: Stripe Prices API — Get business capability. Self-contained, no shared references.
    resources:
    - name: v1-prices
      path: /v1/prices
      operations:
      - name: getprices
        method: GET
        description: Stripe Get   Prices
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: active
          in: query
          type: boolean
          description: Only return prices that are active or inactive (e.g., pass `false` to list all inactive prices).
        - name: created
          in: query
          type: string
          description: 'A filter on the list, based on the object `created` field. The value can be a string with an integer
            Unix timestamp, or it can be a dictionary with a number of '
        - name: currency
          in: query
          type: string
          description: Only return prices for the given currency.
        - name: ending_before
          in: query
          type: string
          description: A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list.
            For instance, if you make a list request and receive 100 ob
        - name: expand
          in: query
          type: array
          description: Specifies which fields in the response should be expanded.
        - name: limit
          in: query
          type: integer
          description: A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default
            is 10.
        - name: lookup_keys
          in: query
          type: array
          description: Only return the price with these lookup_keys, if any exist.
        - name: product
          in: query
          type: string
          description: Only return prices for the given product.
        - name: recurring
          in: query
          type: object
          description: Only return prices with these recurring fields.
        - name: starting_after
          in: query
          type: string
          description: A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list.
            For instance, if you make a list request and receive 100 o
        - name: type
          in: query
          type: string
          description: Only return prices of type `recurring` or `one_time`.
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
    - name: v1-prices-search
      path: /v1/prices/search
      operations:
      - name: getpricessearch
        method: GET
        description: Stripe Get   Prices Search
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: expand
          in: query
          type: array
          description: Specifies which fields in the response should be expanded.
        - name: limit
          in: query
          type: integer
          description: A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default
            is 10.
        - name: page
          in: query
          type: string
          description: A cursor for pagination across multiple pages of results. Don't include this parameter on the first
            call. Use the next_page value returned in a previous respons
        - name: query
          in: query
          type: string
          description: The search query string. See [search query language](https://stripe.com/docs/search#search-query-language)
            and the list of supported [query fields for prices](h
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
    - name: v1-prices-price
      path: /v1/prices/{price}
      operations:
      - name: getpricesprice
        method: GET
        description: Stripe Get   Prices
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: expand
          in: query
          type: array
          description: Specifies which fields in the response should be expanded.
        - name: price
          in: path
          type: string
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
    authentication:
      type: bearer
      token: '{{env.STRIPE_API_KEY}}'
  exposes:
  - type: rest
    namespace: prices-get-rest
    port: 8080
    description: REST adapter for Stripe Prices API — Get. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/v1/prices
      name: v1-prices
      description: REST surface for v1-prices.
      operations:
      - method: GET
        name: getprices
        description: Stripe Get   Prices
        call: prices-get.getprices
        with:
          active: rest.active
          created: rest.created
          currency: rest.currency
          ending_before: rest.ending_before
          expand: rest.expand
          limit: rest.limit
          lookup_keys: rest.lookup_keys
          product: rest.product
          recurring: rest.recurring
          starting_after: rest.starting_after
          type: rest.type
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/v1/prices/search
      name: v1-prices-search
      description: REST surface for v1-prices-search.
      operations:
      - method: GET
        name: getpricessearch
        description: Stripe Get   Prices Search
        call: prices-get.getpricessearch
        with:
          expand: rest.expand
          limit: rest.limit
          page: rest.page
          query: rest.query
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/v1/prices/{price}
      name: v1-prices-price
      description: REST surface for v1-prices-price.
      operations:
      - method: GET
        name: getpricesprice
        description: Stripe Get   Prices
        call: prices-get.getpricesprice
        with:
          expand: rest.expand
          price: rest.price
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: prices-get-mcp
    port: 9090
    transport: http
    description: MCP adapter for Stripe Prices API — Get. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: stripe-get-prices
      description: Stripe Get   Prices
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: prices-get.getprices
      with:
        active: tools.active
        created: tools.created
        currency: tools.currency
        ending_before: tools.ending_before
        expand: tools.expand
        limit: tools.limit
        lookup_keys: tools.lookup_keys
        product: tools.product
        recurring: tools.recurring
        starting_after: tools.starting_after
        type: tools.type
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: stripe-get-prices-search
      description: Stripe Get   Prices Search
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: prices-get.getpricessearch
      with:
        expand: tools.expand
        limit: tools.limit
        page: tools.page
        query: tools.query
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: stripe-get-prices-2
      description: Stripe Get   Prices
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: prices-get.getpricesprice
      with:
        expand: tools.expand
        price: tools.price
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
