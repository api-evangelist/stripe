naftiko: 1.0.0-alpha2
info:
  label: Stripe Payment Links API — Payment Links
  description: 'Stripe Payment Links API — Payment Links. 5 operations. Lead operation: Payment Links. Self-contained Naftiko
    capability covering one Stripe business surface.'
  tags:
  - Stripe
  - Payment Links
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    STRIPE_API_KEY: STRIPE_API_KEY
capability:
  consumes:
  - type: http
    namespace: payment-links-payment-links
    baseUri: https://api.stripe.com
    description: Stripe Payment Links API — Payment Links business capability. Self-contained, no shared references.
    resources:
    - name: v1-payment_links
      path: /v1/payment_links
      operations:
      - name: getpaymentlinks
        method: GET
        description: <p>Returns a list of your payment links.</p>
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: active
          in: query
          type: boolean
          description: Only return payment links that are active or inactive (e.g., pass `false` to list all inactive payment
            links).
        - name: ending_before
          in: query
          type: string
          description: A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list.
            For instance, if you make a list request and receive 100 ob
        - name: expand
          in: query
          type: array
          description: Specifies which fields in the response should be expanded.
        - name: limit
          in: query
          type: integer
          description: A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default
            is 10.
        - name: starting_after
          in: query
          type: string
          description: A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list.
            For instance, if you make a list request and receive 100 o
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
      - name: postpaymentlinks
        method: POST
        description: <p>Creates a payment link.</p>
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    - name: v1-payment_links-payment_link
      path: /v1/payment_links/{payment_link}
      operations:
      - name: getpaymentlinkspaymentlink
        method: GET
        description: <p>Retrieve a payment link.</p>
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: expand
          in: query
          type: array
          description: Specifies which fields in the response should be expanded.
        - name: payment_link
          in: path
          type: string
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
      - name: postpaymentlinkspaymentlink
        method: POST
        description: <p>Updates a payment link.</p>
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: payment_link
          in: path
          type: string
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
    - name: v1-payment_links-payment_link-line_items
      path: /v1/payment_links/{payment_link}/line_items
      operations:
      - name: getpaymentlinkspaymentlinklineitems
        method: GET
        description: <p>When retrieving a payment link, there is an includable <strong>line_items</strong> property containing
          the first handful of those items. There is also a URL where you can retrieve the full (paginated) list of line items.</p>
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: ending_before
          in: query
          type: string
          description: A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list.
            For instance, if you make a list request and receive 100 ob
        - name: expand
          in: query
          type: array
          description: Specifies which fields in the response should be expanded.
        - name: limit
          in: query
          type: integer
          description: A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default
            is 10.
        - name: payment_link
          in: path
          type: string
          required: true
        - name: starting_after
          in: query
          type: string
          description: A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list.
            For instance, if you make a list request and receive 100 o
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
    authentication:
      type: bearer
      token: '{{env.STRIPE_API_KEY}}'
  exposes:
  - type: rest
    namespace: payment-links-payment-links-rest
    port: 8080
    description: REST adapter for Stripe Payment Links API — Payment Links. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/v1/payment-links
      name: v1-payment-links
      description: REST surface for v1-payment_links.
      operations:
      - method: GET
        name: getpaymentlinks
        description: <p>Returns a list of your payment links.</p>
        call: payment-links-payment-links.getpaymentlinks
        with:
          active: rest.active
          ending_before: rest.ending_before
          expand: rest.expand
          limit: rest.limit
          starting_after: rest.starting_after
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
      - method: POST
        name: postpaymentlinks
        description: <p>Creates a payment link.</p>
        call: payment-links-payment-links.postpaymentlinks
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/v1/payment-links/{payment-link}
      name: v1-payment-links-payment-link
      description: REST surface for v1-payment_links-payment_link.
      operations:
      - method: GET
        name: getpaymentlinkspaymentlink
        description: <p>Retrieve a payment link.</p>
        call: payment-links-payment-links.getpaymentlinkspaymentlink
        with:
          expand: rest.expand
          payment_link: rest.payment_link
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
      - method: POST
        name: postpaymentlinkspaymentlink
        description: <p>Updates a payment link.</p>
        call: payment-links-payment-links.postpaymentlinkspaymentlink
        with:
          payment_link: rest.payment_link
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/v1/payment-links/{payment-link}/line-items
      name: v1-payment-links-payment-link-line-items
      description: REST surface for v1-payment_links-payment_link-line_items.
      operations:
      - method: GET
        name: getpaymentlinkspaymentlinklineitems
        description: <p>When retrieving a payment link, there is an includable <strong>line_items</strong> property containing
          the first handful of those items. There is also a URL where you can retrieve the full (paginated) list of line items.</p>
        call: payment-links-payment-links.getpaymentlinkspaymentlinklineitems
        with:
          ending_before: rest.ending_before
          expand: rest.expand
          limit: rest.limit
          payment_link: rest.payment_link
          starting_after: rest.starting_after
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: payment-links-payment-links-mcp
    port: 9090
    transport: http
    description: MCP adapter for Stripe Payment Links API — Payment Links. One tool per consumed operation, routed inline
      through this capability's consumes block.
    tools:
    - name: p-returns-list-your-payment-links
      description: <p>Returns a list of your payment links.</p>
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: payment-links-payment-links.getpaymentlinks
      with:
        active: tools.active
        ending_before: tools.ending_before
        expand: tools.expand
        limit: tools.limit
        starting_after: tools.starting_after
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: p-creates-payment-link-p
      description: <p>Creates a payment link.</p>
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: payment-links-payment-links.postpaymentlinks
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: p-retrieve-payment-link-p
      description: <p>Retrieve a payment link.</p>
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: payment-links-payment-links.getpaymentlinkspaymentlink
      with:
        expand: tools.expand
        payment_link: tools.payment_link
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: p-updates-payment-link-p
      description: <p>Updates a payment link.</p>
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: payment-links-payment-links.postpaymentlinkspaymentlink
      with:
        payment_link: tools.payment_link
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: p-when-retrieving-payment-link-there
      description: <p>When retrieving a payment link, there is an includable <strong>line_items</strong> property containing
        the first handful of those items. There is also a URL where you can retrieve the full (paginated) list of line items.</p>
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: payment-links-payment-links.getpaymentlinkspaymentlinklineitems
      with:
        ending_before: tools.ending_before
        expand: tools.expand
        limit: tools.limit
        payment_link: tools.payment_link
        starting_after: tools.starting_after
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
