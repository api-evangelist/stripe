naftiko: 1.0.0-alpha2
info:
  label: Stripe Payouts API — Reverse
  description: 'Stripe Payouts API — Reverse. 1 operations. Lead operation: Stripe Post   Payouts Reverse. Self-contained
    Naftiko capability covering one Stripe business surface.'
  tags:
  - Stripe
  - Reverse
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    STRIPE_API_KEY: STRIPE_API_KEY
capability:
  consumes:
  - type: http
    namespace: payouts-reverse
    baseUri: https://api.stripe.com
    description: Stripe Payouts API — Reverse business capability. Self-contained, no shared references.
    resources:
    - name: v1-payouts-payout-reverse
      path: /v1/payouts/{payout}/reverse
      operations:
      - name: postpayoutspayoutreverse
        method: POST
        description: Stripe Post   Payouts Reverse
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: payout
          in: path
          type: string
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
    authentication:
      type: bearer
      token: '{{env.STRIPE_API_KEY}}'
  exposes:
  - type: rest
    namespace: payouts-reverse-rest
    port: 8080
    description: REST adapter for Stripe Payouts API — Reverse. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/v1/payouts/{payout}/reverse
      name: v1-payouts-payout-reverse
      description: REST surface for v1-payouts-payout-reverse.
      operations:
      - method: POST
        name: postpayoutspayoutreverse
        description: Stripe Post   Payouts Reverse
        call: payouts-reverse.postpayoutspayoutreverse
        with:
          payout: rest.payout
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: payouts-reverse-mcp
    port: 9090
    transport: http
    description: MCP adapter for Stripe Payouts API — Reverse. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: stripe-post-payouts-reverse
      description: Stripe Post   Payouts Reverse
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: payouts-reverse.postpayoutspayoutreverse
      with:
        payout: tools.payout
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
