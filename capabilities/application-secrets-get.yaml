naftiko: 1.0.0-alpha2
info:
  label: Stripe Application Secrets API — Get
  description: 'Stripe Application Secrets API — Get. 2 operations. Lead operation: Stripe Get Apps Secrets. Self-contained
    Naftiko capability covering one Stripe business surface.'
  tags:
  - Stripe
  - Get
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    STRIPE_API_KEY: STRIPE_API_KEY
capability:
  consumes:
  - type: http
    namespace: application-secrets-get
    baseUri: https://api.stripe.com
    description: Stripe Application Secrets API — Get business capability. Self-contained, no shared references.
    resources:
    - name: v1-apps-secrets
      path: /v1/apps/secrets
      operations:
      - name: getappssecrets
        method: GET
        description: Stripe Get Apps Secrets
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: ending_before
          in: query
          type: string
          description: A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list.
            For instance, if you make a list request and receive 100 ob
        - name: expand
          in: query
          type: array
          description: Specifies which fields in the response should be expanded.
        - name: limit
          in: query
          type: integer
          description: A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default
            is 10.
        - name: scope
          in: query
          type: object
          description: Specifies the scoping of the secret. Requests originating from UI extensions can only access account-scoped
            secrets or secrets scoped to their own user.
          required: true
        - name: starting_after
          in: query
          type: string
          description: A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list.
            For instance, if you make a list request and receive 100 o
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
    - name: v1-apps-secrets-find
      path: /v1/apps/secrets/find
      operations:
      - name: getappssecretsfind
        method: GET
        description: Stripe Get Apps Secrets Find
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: expand
          in: query
          type: array
          description: Specifies which fields in the response should be expanded.
        - name: name
          in: query
          type: string
          description: A name for the secret that's unique within the scope.
          required: true
        - name: scope
          in: query
          type: object
          description: Specifies the scoping of the secret. Requests originating from UI extensions can only access account-scoped
            secrets or secrets scoped to their own user.
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
    authentication:
      type: bearer
      token: '{{env.STRIPE_API_KEY}}'
  exposes:
  - type: rest
    namespace: application-secrets-get-rest
    port: 8080
    description: REST adapter for Stripe Application Secrets API — Get. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/v1/apps/secrets
      name: v1-apps-secrets
      description: REST surface for v1-apps-secrets.
      operations:
      - method: GET
        name: getappssecrets
        description: Stripe Get Apps Secrets
        call: application-secrets-get.getappssecrets
        with:
          ending_before: rest.ending_before
          expand: rest.expand
          limit: rest.limit
          scope: rest.scope
          starting_after: rest.starting_after
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/v1/apps/secrets/find
      name: v1-apps-secrets-find
      description: REST surface for v1-apps-secrets-find.
      operations:
      - method: GET
        name: getappssecretsfind
        description: Stripe Get Apps Secrets Find
        call: application-secrets-get.getappssecretsfind
        with:
          expand: rest.expand
          name: rest.name
          scope: rest.scope
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: application-secrets-get-mcp
    port: 9090
    transport: http
    description: MCP adapter for Stripe Application Secrets API — Get. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: stripe-get-apps-secrets
      description: Stripe Get Apps Secrets
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: application-secrets-get.getappssecrets
      with:
        ending_before: tools.ending_before
        expand: tools.expand
        limit: tools.limit
        scope: tools.scope
        starting_after: tools.starting_after
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: stripe-get-apps-secrets-find
      description: Stripe Get Apps Secrets Find
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: application-secrets-get.getappssecretsfind
      with:
        expand: tools.expand
        name: tools.name
        scope: tools.scope
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
