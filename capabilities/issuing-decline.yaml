naftiko: 1.0.0-alpha2
info:
  label: Stripe Issuing API — Decline
  description: 'Stripe Issuing API — Decline. 1 operations. Lead operation: Stripe Post   Issuing Authorizations Decline.
    Self-contained Naftiko capability covering one Stripe business surface.'
  tags:
  - Stripe
  - Decline
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    STRIPE_API_KEY: STRIPE_API_KEY
capability:
  consumes:
  - type: http
    namespace: issuing-decline
    baseUri: https://api.stripe.com
    description: Stripe Issuing API — Decline business capability. Self-contained, no shared references.
    resources:
    - name: v1-issuing-authorizations-authorization-decline
      path: /v1/issuing/authorizations/{authorization}/decline
      operations:
      - name: postissuingauthorizationsauthorizationdecline
        method: POST
        description: Stripe Post   Issuing Authorizations Decline
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: authorization
          in: path
          type: string
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
    authentication:
      type: bearer
      token: '{{env.STRIPE_API_KEY}}'
  exposes:
  - type: rest
    namespace: issuing-decline-rest
    port: 8080
    description: REST adapter for Stripe Issuing API — Decline. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/v1/issuing/authorizations/{authorization}/decline
      name: v1-issuing-authorizations-authorization-decline
      description: REST surface for v1-issuing-authorizations-authorization-decline.
      operations:
      - method: POST
        name: postissuingauthorizationsauthorizationdecline
        description: Stripe Post   Issuing Authorizations Decline
        call: issuing-decline.postissuingauthorizationsauthorizationdecline
        with:
          authorization: rest.authorization
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: issuing-decline-mcp
    port: 9090
    transport: http
    description: MCP adapter for Stripe Issuing API — Decline. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: stripe-post-issuing-authorizations-decline
      description: Stripe Post   Issuing Authorizations Decline
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: issuing-decline.postissuingauthorizationsauthorizationdecline
      with:
        authorization: tools.authorization
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
